# Server-only secrets
OPENAI_API_KEY=
# Optional overrides; Responses API routes default to gpt-5.6-sol.
OPENAI_AGENT_MODEL=
OPENAI_FOOD_MODEL=
OPENAI_VISION_MODEL=
# Recorded speech-to-text defaults to the recommended gpt-transcribe model.
OPENAI_TRANSCRIPTION_MODEL=
# Upstash Redis REST credentials for friend sync and push-token storage.
# Legacy KV_REST_API_URL / KV_REST_API_TOKEN names are also supported.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Public origins. The native bundle uses NEXT_PUBLIC_API_ORIGIN for AI,
# friend sync, and APNs token registration.
NEXT_PUBLIC_SITE_URL=https://your-web-app.example
NEXT_PUBLIC_API_ORIGIN=https://your-web-app.example

# Optional build-only alias used by `npm run cap:build`.
CAPACITOR_API_ORIGIN=https://your-web-app.example

# Apple Push Notification service (APNs). Store the .p8 contents with literal
# \n escapes when your host does not support multiline environment values.
APNS_KEY_ID=
APNS_TEAM_ID=KF7Y4XG4SG
APNS_PRIVATE_KEY=
APNS_BUNDLE_ID=com.melonmate.app
# Use true on Vercel for TestFlight/App Store builds; false targets Xcode development tokens.
APNS_PRODUCTION=false
PUSH_ADMIN_SECRET=
